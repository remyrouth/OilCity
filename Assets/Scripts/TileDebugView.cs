using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class TileDebugView : MonoBehaviour
{
 /*   private void OnDrawGizmos()
    {
        if (!Application.isPlaying) return;

        for (int x = 0; x < BoardManager.MAP_SIZE_X; ++x)
        {
            for (int y = 0; y < BoardManager.MAP_SIZE_Y; ++y)
            {
                Gizmos.color = Color.red;
                Gizmos.DrawWireCube(new Vector3(x, y) + new Vector3(0.5f, 0.5f), new Vector3(1f, 1f, 0.1f));
                UnityEditor.Handles.Label(new Vector3(x + 0.1f, y + 0.1f), string.Format("({0}, {1})", x, y));
            }
        }
    }*/
}
